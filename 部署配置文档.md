# E-LOG 企业级日志分析系统 - 部署配置文档

## 概述

本文档记录 `e-log-analysis` 项目在 CentOS 7.6 虚拟机上的完整部署和配置过程。

---

## 一、项目结构

```
e-log-analysis/
├── src/
│   └── main/
│       ├── java/cn/edu/tjrac/
│       │   ├── common/          # 公共类（异常、日志、AOP）
│       │   ├── config/          # 配置类（Flink、Spark、HDFS）
│       │   ├── controller/      # REST API 控制器
│       │   ├── dao/             # 数据访问层（Flink/Spark/Flume）
│       │   ├── entity/          # 实体类
│       │   ├── flink/           # Flink 作业
│       │   ├── service/         # 服务层
│       │   ├── task/            # Spark 任务
│       │   └── vo/              # 值对象
│       └── resources/
│           └── application.yml  # 主配置文件
├── frontend/                    # Vue 3 前端
│   ├── src/
│   │   ├── App.vue             # 主组件（含3D背景）
│   │   ├── api/index.js       # API 请求封装
│   │   └── components/         # 组件
│   ├── index.html             # 入口 HTML
│   ├── package.json
│   └── vite.config.js         # Vite 配置（含 API 代理）
├── docker/                     # Docker 配置文件
│   ├── flume/conf/            # Flume 配置
│   ├── nginx/                 # Nginx 配置
│   └── spark/jobs/            # Spark 作业
├── pom.xml                    # Maven 依赖配置
└── 部署配置文档.md             # 本文档
```

---

## 二、环境信息

### 2.1 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | 192.168.146.128 |
| 操作系统 | CentOS 7.9.2009 |
| 内存 | 8GB+ |
| Docker | 已安装 |

### 2.2 已安装组件版本

| 组件 | 版本 | 路径 |
|------|------|------|
| JDK | 21 | /opt/jdk-21.0.11 |
| Maven | 3.9.6 | /opt/apache-maven-3.9.6 |
| Node.js | 18.20.0 | /opt/node-v18.20.0-linux-x64 |

---

## 三、数据来源

### 3.1 GitHub Event Fetcher（主要数据源）

系统通过 `GitHubEventFetcher` 服务从 GitHub API 实时抓取公开事件：

- **API 地址**: `https://api.github.com/events`
- **抓取间隔**: 3 秒
- **数据格式**:
```json
{
  "endpoint": "/github/{user}/{repo}",
  "method": "PushEvent|PullRequestEvent|IssueEvent",
  "responseTime": 200,
  "ip": "user:{username}",
  "timestamp": 1777453027746,
  "statusCode": 200
}
```

### 3.2 数据流向

```
GitHub API (api.github.com/events)
    ↓
GitHubEventFetcher (每3秒抓取)
    ↓
Kafka (192.168.146.128:9092, topic: log-topic)
    ↓
Flink Streaming (实时处理)
    ↓
HDFS (flink-job.jar 处理结果)
    ↓
Spring Boot API (8089端口)
    ↓
Vue Frontend (5173端口)
```

---

## 四、Docker 大数据集群

### 4.1 集群架构 (docker-compose)

使用 `project-docker` 目录下的 docker-compose.yml 部署，核心服务：

| 服务 | 容器名 | 端口 | 说明 |
|------|--------|------|------|
| Zookeeper | project-docker-zookeeper-1 | 2181 | Kafka 协调服务 |
| Kafka | project-docker-kafka-1 | 9092 | 消息队列 |
| NameNode | project-docker-namenode-1 | 9000, 9870 | HDFS 主节点 |
| DataNode | project-docker-datanode-1 | 9864, 9866 | HDFS 数据节点 |
| Flume | project-docker-flume-1 | 44444 | 日志采集 |

### 4.2 关键配置

#### Kafka 配置 (docker-compose.yml)
```yaml
kafka:
  environment:
    KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
    KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
    KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://192.168.146.128:9092
```

#### Flume 配置 (flume.conf)
```properties
agent.sources.http-source.type = http
agent.sources.http-source.port = 44444
agent.sinks.kafka-sink.type = org.apache.flume.sink.kafka.KafkaSink
agent.sinks.kafka-sink.kafka.bootstrap.servers = 192.168.146.128:9092
agent.sinks.kafka-sink.kafka.topic = log-topic
```

---

## 五、Flink 实时处理集群

### 5.1 Flink 容器启动

```bash
# 启动 JobManager
docker run -d --name log-flink \
  --network log-analysis-platform_log-network \
  -p 8081:8081 \
  -e FLINK_MODE=jobmanager \
  -e JOB_MANAGER_RPC_ADDRESS=log-flink \
  flink:1.17.1 jobmanager

# 启动 TaskManager
docker run -d --name log-taskmanager \
  --network log-analysis-platform_log-network \
  -e JOB_MANAGER_RPC_ADDRESS=log-flink \
  flink:1.17.1 taskmanager
```

### 5.2 提交 Flink 作业

```bash
# 打包 Flink 作业 JAR（从源码编译）
mvn clean package -DskipTests

# 复制 JAR 到容器
docker cp target/flink-job.jar log-flink:/tmp/flink-job.jar

# 提交作业
docker exec log-flink /opt/flink/bin/flink run -d /tmp/flink-job.jar
```

### 5.3 Flink UI 访问

- **地址**: http://192.168.146.128:8081
- **作业状态**: RUNNING

---

## 六、Spring Boot 后端配置

### 6.1 当前状态

| 项目 | 值 |
|------|------|
| JAR 包位置 | /tmp/e-log-ca-system.jar |
| 运行端口 | 9090 |
| 启动命令 | java -jar e-log-ca-system.jar --server.address=0.0.0.0 |
| 日志文件 | /tmp/app.log |

### 6.2 application.yml 配置

```yaml
server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/log_analysis
    username: root
    password: 123456

bigdata:
  kafka:
    bootstrap-servers: 192.168.146.128:9092
    topic: log-topic
    group-id: log-analysis-group
  hdfs:
    uri: hdfs://192.168.146.128:9000
    base-path: /log-analysis
```

### 6.3 关键修复配置

#### 修复 1: hosts 文件配置
```bash
echo '127.0.0.1 namenode' >> /etc/hosts
echo '127.0.0.1 kafka' >> /etc/hosts
```

#### 修复 2: Kafka Topic 创建
```bash
docker exec -it project-docker-kafka-1 kafka-topics \
  --create --topic log-topic \
  --bootstrap-server localhost:9092 \
  --partitions 1 --replication-factor 1
```

---

## 七、前端配置

### 7.1 前端技术栈

- Vue 3 + Vite
- ECharts 5（图表）
- Three.js（3D 背景）
- Axios（API 请求）

### 7.2 Vite 配置 (vite.config.js)

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    host: true,
    proxy: {
      '/api': {
        target: 'http://192.168.146.128:9090',
        changeOrigin: true,
        secure: false
      }
    }
  }
})
```

### 7.3 API 请求代理

前端 `/api/*` 请求通过 Vite 代理转发到后端 `http://192.168.146.128:9090`

---

## 八、验证命令

### 8.1 检查服务状态

```bash
# Docker 容器状态
docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'

# Java 后端进程
ps aux | grep e-log-ca-system

# Flink 作业状态
curl -s http://localhost:8081/jobs
```

### 8.2 测试 API

```bash
# 后端健康检查
curl http://localhost:9090/api/health

# 查看数据概览
curl http://localhost:9090/api/overview

# 查看日志级别
curl http://localhost:9090/api/log-levels

# 查看 HDFS Dashboard
curl http://localhost:9090/api/hdfs/dashboard
```

### 8.3 Kafka 消费者测试

```bash
# 查看实时数据
docker exec project-docker-kafka-1 kafka-console-consumer \
  --bootstrap-server localhost:9092 \
  --topic log-topic --from-beginning --max-messages 3
```

---

## 九、启动顺序

### 9.1 虚拟机端

1. **启动 Docker 集群**
   ```bash
   cd /opt/project-docker
   docker compose up -d
   ```

2. **创建 Kafka Topic**
   ```bash
   docker exec project-docker-kafka-1 kafka-topics \
     --create --topic log-topic --bootstrap-server localhost:9092 \
     --partitions 1 --replication-factor 1
   ```

3. **启动 Flink 集群**
   ```bash
   # JobManager
   docker run -d --name log-flink \
     --network log-analysis-platform_log-network \
     -p 8081:8081 -e FLINK_MODE=jobmanager \
     -e JOB_MANAGER_RPC_ADDRESS=log-flink \
     flink:1.17.1 jobmanager

   # TaskManager
   docker run -d --name log-taskmanager \
     --network log-analysis-platform_log-network \
     -e JOB_MANAGER_RPC_ADDRESS=log-flink \
     flink:1.17.1 taskmanager
   ```

4. **提交 Flink 作业**
   ```bash
   docker cp target/flink-job.jar log-flink:/tmp/
   docker exec log-flink /opt/flink/bin/flink run -d /tmp/flink-job.jar
   ```

5. **启动 Spring Boot**
   ```bash
   cd /tmp
   nohup java -jar e-log-ca-system.jar --server.address=0.0.0.0 > /tmp/app.log 2>&1 &
   ```

### 9.2 本地开发机端

1. **安装依赖**
   ```bash
   cd frontend
   npm install
   ```

2. **启动前端**
   ```bash
   npm run dev
   ```

3. **访问页面**
   - 前端: http://localhost:5173/
   - 后端 API: http://192.168.146.128:9090/

---

## 十、API 接口列表

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/overview` | GET | 数据概览（PV/UV/错误数/响应时间） |
| `/api/pvuv` | GET | PV/UV 统计 |
| `/api/topN` | GET | Top N API |
| `/api/errors` | GET | 错误日志 |
| `/api/health` | GET | 系统健康状态 |
| `/api/servers` | GET | 服务器状态 |
| `/api/log-levels` | GET | 日志级别分布 |
| `/api/hdfs/pv` | GET | HDFS PV 数据 |
| `/api/hdfs/uv` | GET | HDFS UV 数据 |
| `/api/hdfs/errors` | GET | HDFS 错误统计 |
| `/api/hdfs/rt` | GET | HDFS 响应时间分布 |
| `/api/hdfs/top` | GET | HDFS Top API |
| `/api/hdfs/dashboard` | GET | HDFS 实时仪表盘 |
| `/api/hdfs/dirs` | GET | HDFS 目录列表 |

---

## 十一、访问地址汇总

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | http://localhost:5173/ | Vue 3 仪表盘 |
| Spring Boot 后端 | http://192.168.146.128:9090 | 主 API |
| HDFS Web UI | http://192.168.146.128:9870 | HDFS 管理界面 |
| Flume HTTP | http://192.168.146.128:44444 | 日志接收端口 |
| Kafka | 192.168.146.128:9092 | 消息队列 |
| Flink UI | http://192.168.146.128:8081 | Flink 管理界面 |

---

## 十二、已知问题与解决方案

### 12.1 HDFS NoRouteToHost

**问题**: `java.net.NoRouteToHostException: No route to host`

**解决**: 添加 hosts 记录
```bash
echo '127.0.0.1 namenode' >> /etc/hosts
```

### 12.2 Flink 容器 Exited (137)

**问题**: Flink JobManager 和 TaskManager 容器内存不足被 kill

**解决**: 使用 flink:1.17.1 镜像替代 custom-flink，并正确设置 FLINK_MODE 环境变量

### 12.3 Kafka Topic 不存在

**问题**: 日志发送失败，Flume 无法连接 Kafka

**解决**: 手动创建 topic
```bash
docker exec project-docker-kafka-1 kafka-topics \
  --create --topic log-topic --bootstrap-server localhost:9092 \
  --partitions 1 --replication-factor 1
```

---

## 十三、本次修改记录

### 2026-04-29 修改

1. **前端修复**
   - 创建 `index.html` 入口文件
   - 创建 `vite.config.js` 配置代理
   - 整理 `App.vue` CSS 样式

2. **Flink 集群重新部署**
   - 使用 flink:1.17.1 官方镜像
   - 手动启动 JobManager 和 TaskManager
   - 提交 flink-job.jar 作业

3. **配置更新**
   - 更新 hosts 文件配置
   - 更新 Kafka Topic 创建
   - 更新启动顺序文档

---

*文档更新时间: 2026-04-29*
